import { platformOffiaccount } from './PlatformOffiaccount';

describe('platformOffiaccount', () => {
  it('should work', () => {
    expect(platformOffiaccount()).toEqual('PlatformOffiaccount');
  });
});
